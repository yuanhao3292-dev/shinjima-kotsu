# ================================
# Stripe 测试环境配置
# ================================
# 从 https://dashboard.stripe.com/test/apikeys 获取
# 注意：确保 Dashboard 切换到【测试模式】

# 可发布密钥（前端使用，以 pk_test_ 开头）
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_YOUR_KEY_HERE"

# 密钥（后端使用，以 sk_test_ 开头）
STRIPE_SECRET_KEY="sk_test_YOUR_KEY_HERE"

# Webhook 密钥（本地测试时使用 Stripe CLI）
STRIPE_WEBHOOK_SECRET="whsec_YOUR_LOCAL_WEBHOOK_SECRET"

# ================================
# 如何使用：
# ================================
# 1. 在 Stripe Dashboard 获取测试密钥后，替换上面的 YOUR_KEY_HERE
# 2. 备份当前的 .env.local：cp .env.local .env.local.backup
# 3. 将此文件内容复制到 .env.local
# 4. 重启开发服务器：npm run dev
