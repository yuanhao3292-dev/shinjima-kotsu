# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next/
out/

# TypeScript 增量编译缓存（3MB 级产物，不应进版本库）
*.tsbuildinfo

# Environment variables
# 注意：下面用的是通配前缀，而不是 .env*.local ——
# 曾经有 .env.local.stripe-test 因为 .local 不在结尾而漏网被提交。
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local

# Backup files
*.backup
*.bak
*~

# Env check files (may contain secrets)
.env.check
.env.production.check
.env*.check

# DPC Excel data (large, downloaded from MHLW)
data/dpc/*.xlsx

# Playwright 本地运行产物
/test-results/
/playwright-report/
/blob-report/
